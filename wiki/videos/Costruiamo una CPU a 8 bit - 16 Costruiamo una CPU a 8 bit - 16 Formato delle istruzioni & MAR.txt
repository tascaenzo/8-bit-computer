Salve a tutti e bentornati sul mio canale YouTube. Oggi continuiamo a parlare della CPU a 8 bit. Nell'ultimo video abbiamo visto il confronto tra architettura di Harvard ed architettura Evon Newman. Abbiamo detto che utilizzeremo l'architettura Evon Newman per realizzare la nostra CPU. Nel video di oggi vorrei entrare più nel dettaglio e vedere tutti i passaggi e tutti i registri che vengono utilizzati durante l'esecuzione di un'istruzione da parte della nostra CPU. Ma prima di iniziare vi ricordo di lasciare un mi piace, commentare questo video e iscrivervi al mio canale per supportare il mio lavoro. Prima di iniziare vorrei vedere come organizziamo le istruzioni all'interno della memoria. Partiamo subito col dire che una CPU a 8 bit utilizza 8 bit di informazione per andare a gestire i dati che dovrà processare. Il problema è che con 8 bit di informazione possiamo gestire un massimo di 256 combinazioni che effettivamente sono pochine. In effetti eh ci sono tutta una serie di istruzioni che potrebbero aver bisogno più di 8 bit per essere rappresentate. Ad esempio, facciamo il caso in cui dobbiamo caricare un valore noto all'interno di un registro, avremo bisogno di 8 bit di informazione solo per il valore da caricare più avremo bisogno di 3 bit per selezionare quale dei registri generali dovremmo utilizzare. Poi un bit servirà per dire che siamo in lettura o in scrittura e quindi comunque vedete che già abbiamo più di 8 bit per questa semplice istruzione. In effetti una soluzione che è stata adoperata storicamente nelle CPU a 8 bit è quella di avere istruzioni con un numero di bit che varia a seconda della tipologia di istruzione. Io ho pensato di raggruppare le istruzioni in tre macro categorie. ci sono delle istruzioni che avranno, allora diciamo che l'istruzione avrà sempre un codice operazione a 8 bit che va a identificare il tipo di operazione. Ad esempio operazioni più semplici come il reset di un registro con 8 bit di informazione eh possiamo, diciamo, rappresentare questo tipo di istruzione e tutto ciò che eh serve per andarla a rappresentare. Poi ci saranno istruzioni che prevederanno un immediato. L'immediato, in sostanza, è un valore noto. Ad esempio, come abbiamo già detto, caricare un valore all'interno di un registro. Questa è un'istruzione che utilizzerà 2 byte di informazione, 15 16 bit. Abbiamo quindi il codice operazione e poi 8 bit relativa all'immediato. Poi ci saranno istruzioni che ehm, diciamo, andranno a prevedere un indirizzo di memoria. Ad esempio, se dobbiamo fare una scrittura in memoria, una, oppure dobbiamo leggere un valore dalla memoria, oppure un salto condizionale, queste istruzioni avranno bisogno di un indirizzo di memoria. Quindi avremo 8 bit di informazione del codice operazione e 16 bit che vanno a rappresentare l'indirizzo di memoria a cui dovremo puntare. Quindi diciamo subito che avremo operazioni da 8 bit, 16 bit e 24 bit. Sicuramente già leggendo i primi 8 bit del codio operazione capiamo quante letture dovrà fare la CPU all'interno della memoria. Questo qua va a variare la fase di fetch, quindi la fase eh in cui la CPU va a reperire l'informazione dalla memoria. Ho rappresentato qui la memoria con questa tabella e ho fatto un paio d' esempi. Ad esempio, l'istruzione 1 avrà un codice operazione, poi avrà la prima parte dell'indirizzo di memoria eh relativa a quell'istruzione e poi la seconda parte. Quindi abbiamo tre ehm byte di informazione che vanno a racchiudere l'istruzione 1. L'istruzione 2 invece avrà soltanto un codice operazione e un immediato, quindi avrà 2 byte di informazione e così via per tutte le altre istruzioni. Allora, già da qui abbiamo definito che l'indirizzo, il basso di indirizzi della memoria è a 16 bit, anche se poi effettivamente abbiamo visto nel video precedente che la RAM e la EPROM non utilizzeranno 16 linea indirizzo, hanno meno linea indirizzo, però noi predisponiamo la CPU per lavorare a 16 bit per l'indirizzamento della memoria. Poi, male che va non utilizzeremo tutti i bit indirizzare la memoria. Ho disegnato un piccolo schema che va a rappresentare tutti i vari registri, la memoria e ci permetterà di capire qual è il flusso di lavoro durante l'esecuzione di un'operazione all'interno della CPU. Abbiamo la nostra memoria, abbiamo un bass di indirizzi a 16 bit che serve a selezionare la riga da leggere all'interno della memoria. Poi abbiamo il Bass dati, il classico Bass 8 bit che abbiamo utilizzato nei video precedenti, a cui collegheremo l'istruction register, il memory data register e poi esso sarà collegato all'interno della nostra RAM che potrà scrivere o leggere all'interno di questo buss. Che cosa succede? Succede che l'istruction register conterrà i primi 8 bit dell'istruzione, il nostro codice operazione per intenderci, che ci permette di capire effettivamente qual è l'operazione che deve essere eseguita dalla CPU. L'istruction register poi a sua volta è collegata alla Control Unit che nella fase di decode andrà a generare tutti i segnali di controllo che serviranno nella alla CPU. Poi abbiamo il memory data register, va a contenere un valore che può essere letto dalla memoria oppure un valore che dovremmo andare a scrivere all'interno della memoria. E poi abbiamo il program counter e il memory address register che sono registri a 16 bit. In effetti l'uscita è collegata al bass degli indirizzi, quindi saranno i due registri che andranno poi fisicamente a pilotare la memoria, quindi a selezionare la riga all'interno della nostra memoria. Qual è il problema? Il problema è che il program counter e il memory address register vanno a prendere l'indirizzo dal da data dal bass dati a 8 bit. In sostanza, ad esempio, eh sono questi 16 bit che vanno a rappresentarmi un indirizzo. Però per andare a popolare il MAR o il program counter dovremmo fare due letture. Dobbiamo passare da un da un basti a 8 bit. Quindi questi due registri saranno divisi in due parti, saranno due registri a 8 bit e andremo a ehm ad avere la parte alta del MAR e la parte bassa del MAR con i bit meno significativi e i biti. Lo stesso vale per il program counter. È come se spezziamo questi due registri da 16 bit in registri da 8 bit. Ad esempio, per andare a prendere questo valore e salvarlo all'interno del MAR, dovremmo fare eh dovremmo avere sicuramente due cicli di clock, perché nel primo andremo a popolare la parte bassa del del mar e poi la parte alta del mar nel secondo ciclo di clock. Ora qui sorge un problema. Il problema è che il program counter, che è un registro che deve autoincrementarsi di uno per andare avanti sull'esecuzione del programma, potrebbe, nel caso in cui si verifica un salto condizionale, potrebbe essere dover essere caricato a un indirizzo specifico che si trova ad esempio in questi eh in questi biti, in queste due righe. Per fare ciò, che cosa potremmo pensare? che andiamo a prendere eh questo valore, lo andiamo a salvare all'interno della parte meno significativa del program counter, facciamo un incremento e andiamo a prendere la seconda parte, la salviamo nella nella parte nel più significativa del program counter. Il problema è che quando noi andiamo a prendere questo valore, lo salviamo dal programo counter, andiamo a corrompere eh il l'indirizzo originale dell'istruzione in esecuzione, quindi abbiamo bisogno sicuramente di un registro temporaneo che mi permette di eh prendere questi due valori, caricarli nel registro temporaneo e poi quando abbiamo tutto ehm l'informazione a 16 bit la carico all'interno del program counter A quel punto il program counter farà il salto condizionale eh verso una nuova area di memoria. Pensavo che potremmo utilizzare il MAR, potremmo caricare tutto nel MAR e poi decidere se utilizzare il MAR per pilotare la memoria nel caso in cui dobbiamo, ad esempio, allegere una variabile o scrivere all'interno della variabile, oppure prendere questa informazione e caricarla nel program e poi il program counter andrà a pilotare la nostra eh memoria. Quindi, tecnicamente è sbagliato questo collegamento perché noi dovremmo prendere e collegarci all'uscita del MAR e inserirla in ingresso nel program. A questo punto il primo registro che andremo a realizzare è il MAR M mem Memory Address Register. Utilizzeremo 274574 che abbiamo già utilizzato in altri video, ad esempio per tutti i registri generali o comunque in generale tutti i registri che abbiamo analizzato finora e come dicevamo dobbiamo collegarli in modo da poter avere una parte bassa, gli 8 bit meno significativi e una parte alta, gli 8 bit più significativi. Come ho fatto tutto il collegamento, innanzitutto distinguiamo questi 16 bit che saranno il nostro bass di indirizzi che piloterà la memoria. Poi abbiamo il bass dati a 8 bit da cui andremo a reperire le informazioni per caricarle all'interno del registro. Eh, diciamo che i nostri registri avranno l'input e l'output e poi avranno il clock che ci permette di eh salvare l'informazione. Quindi al fronte di salita del clock noi salviamo l'informazione che sta transitando su data. Che cosa facciamo? Utilizziamo due segnali, almeno per il momento, che saranno WR Light, che mi permetteranno di salvare all'interno della parte bassa e parte alta del registro a 16 bit. Mandiamo questi due ingressi, questi due segnali in due porte end. Nell'altro capo della porta end andiamo a selezionare il clock. Al solito, quando eh questo uno dei due sarà a livello logico alto, avremo il clock in uscita e quindi faremo un salvataggio, cioè salveremo l'informazione che transita all'interno del bass dati. Qui di fianco ho anche scritto l'uscita che va a comporre i 16 bitindi indirizzo. Dalla parte bassa, il marl avremo le uscite che vanno da 7 a 0. sulla parte superiore alta le uscite che vanno da A15 ad A8. Adesso vediamo effettivamente come ho realizzato questa parte all'interno della board e facciamo anche qualche piccolo test. Qui possiamo vedere la realizzazione del circuito. Allora, abbiamo un bass di indirizzi che è fatto da 16 bit, il nostro bass di indirizzi, abbiamo 16 LED. Questi LED sono stati collegati con delle resistenze per limitare la corrente che scorre al loro interno. A questi LED ho collegato direttamente al momento l'uscita del MAR. Il MAR è è costituito da questi due registri che sono dei 74574. Questo va a rappresentarmi la parte bassa, i primi 8 bit e questi i successivi 8 bit che sono la parte alta di informazione. Poi gli ingressi di questi due registri vengono presi dal bass dati che vado a prelevarmeli dalla blocco dei registri generali. Qui sotto invece ho un 7408, contiene delle porte endare l'end tra il clock e questi due cavi gialli che sono i segnali di scrittura che vanno a pilotarmi il salvataggio delle informazioni su Mar Love e Marict. E poi nient'altro. Qui ci sono tutta un'altra serie di integrati che vedremo nel prossimo video. Questi integrati qua e questi due che mi serviranno per il program counter. li ho già montati per avere un'idea degli ingombri e degli spazi all'interno della board. Ho cambiato inquadratura in modo da avere una panoramica di tutto il circuito. Proviamo a dare alimentazione. Ok, ho alimentato il circuito. Che cosa dobbiamo fare? Dobbiamo provare a prendere un'informazione che sta transitando sul bass dati e caricarla all'interno del MAR. Quindi il ciclo che poi verrà utilizzato effettivamente dalla CPU è quello di caricare i primi 8 bit e i successivi 8 bitta. Per fare ciò andiamo a pilotare il bass con il nostro switch che va a rappresentare la nostra periferica di input. Ok, proviamo ad accendere tutti i bit. A questo punto andiamo a mettere a + 5 V il segnale Wright della parte bassa del MAR e diamo un impulso di clock. Che cosa succede? Dovrebbero accendersi i primi 8 bit del bass dati, del bass degli indirizzi. do un impulso di clock ed effettivamente ho fatto una scrittura all'interno della parte bassa del MAR e quindi abbiamo i primi 8 bit accesi. A questo punto vado a mettere a + 5 V il segnale di scrittura del MAR parte alta. Il la parte bassa invece è messo a massa. Eh, diamo un altro impulso di clock e dovrebbero accendersi i gli 8 bit più significativi. Effettivamente è vero. Quindi, che cosa abbiamo fatto? Abbiamo caricato un valore all'interno del MAR in due, diciamo, facendo due scritture separate, in due momenti separati, in modo da popolare un registro a 16 bit, eh, che è stato diviso in due sottorregistri da 8 bit, quindi parte bassa e parte alta. Diciamo che per il momento è tutto. Nel prossimo video proviamo a completare, a costruire il program counter e poi una volta che abbiamo il program counter e il MAR operativi, possiamo provare effettivamente a collegare la RAM e provare a fare qualche scrittura e lettura all'interno della della RAM. Diciamo che per questo video è tutto.