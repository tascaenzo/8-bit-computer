Salve a tutti e bentornati sul mio canale YouTube. Oggi faremo un video un po' più teorico. Parleremo di architettura di Von Newan, architettura di Harvard e faremo una breve introduzione alla memoria. Ma prima di iniziare vi ricordo di lasciare un mi piace, commentare questo video e iscrivervi al mio canale in modo da supportare il mio lavoro. Prima di parlare dell'architettura di Von Newman e dell'architettura di Harvard, vorrei fare una panoramica sulle memorie. Possiamo immaginare la memoria come una sorta di tabella contenente righe e colonne al cui interno sono storate delle informazioni. Ho rappresentato l'analogia appena descritta in questo schema. Di fatto è una tabella, abbiamo delle righe. Eh, all'interno di ogni riga ci sono 8 bit di informazioni, quindi possiamo immaginare come otto celle. Eh, ad esempio, se vado a prendere la prima riga o l'informazione 01 1 1 1 e così via per tutte le successive fino ad arrivare all'ennesima riga. Di fatto tramite un bass di indirizzo vado a selezionare una determinata riga che poi sarà la nostra locazione di memoria. Una volta che ho selezionato la riga potrò leggere l'informazione all'interno dell'uscita data della nostra memoria. In realtà potrei anche scrivere all'interno della memoria se eh il tipo di memoria consente una scrittura. Quindi di fatto possiamo schematizzare tutto con una sorta di device, con un bass di indirizzo che mi permette di selezionare una locazione di memoria e un'uscita che mi mostra il dato presente all'interno di quella locazione di memoria. Esistono varie tipologie di memoria, però oggi vorrei soffermarmi soltanto sulle ROM e le RAM. Le ROM sono delle memorie di sola lettura. Di fatto ROM sta per read only memory. Storicamente, ad esempio, i BIOS del dei computer sono delle ROM. Inizialmente questa tipologia di memoria veniva programmata, veniva costruita programmandolo con un'informazione che poi era fissa nel tempo e non poteva essere mai modificata. Successivamente ci sono state delle evoluzioni e siamo passati alle eprom e poi alle eeprom che sono delle memorie che possono essere programmate ehm prima di essere poi utilizzate come in sola lettura. Ad esempio, EPROM sta per Erasable Programmable Read Only Memory. Che cosa vuol dire? Vuol dire che abbiamo una sorta di eh finestrella all'interno del chip che se è esposta a raggi ultravioletti cancellava la memoria. Una volta cancellata la memoria, possiamo riprogrammarla tramite eh degli impulsi elettrici. Eh, in effetti di solito questa finestrella veniva esposta, veniva cancellata, poi veniva coperta per fare in modo che eh non si degradasse, non si danneggiasse l'informazione contenuta all'interno della memoria, veniva programmata e poi veniva utilizzata. Invece un'evoluzione successiva è la eepeprom. Di fatto è una memoria che può essere cancellata tramite degli impulsi elettrici e poi riprogrammata. Un esempio è la T28 C64. È una EEPROM che utilizzeremo nel nostro progetto. Di fatto ha 13 linee di indirizzo. Che cosa vuol dire? Vuol dire che abbiamo 2 elevato 13 che fa 8.192 bit di informazione. Quindi è come se all'interno del bass di indirizzi abbiamo un numero che va da 0 a 8.191 indirizzi. A ogni indirizzo poi corrisponde una riga che sono i nostri 8 bit informazione. Di fatto abbiamo 8 KB di informazione. Mi raccomando la bigola sta per byte perché ogni eh ogni indirizzo va a puntare a 8 bit di informazione. 8 bit informazioni vanno a costituire 1 byte e da questo Kilby. A questo punto possiamo parlare del RAM. RAM ST per Random Access Memory sono memorie di tipo volatile. Vuol dire che se io tolgo l'alimentazione perdo l'informazione che avevo salvato al suo interno. Sono utilizzate dalla CPU come memorie temporanee, memorie di lavoro. Diciamo che la CPU potrà leggere e scrivere all'interno di essa. Random access memory sta per sta a significare che la CPU può leggere qualsiasi locazione di memoria, qualsiasi indirizzo nello stesso tempo. Quindi, ad esempio, se la CPU legge l'informazione contenuta all all'indirizzo 2 o l'informazione contenuta all'indirizzo N, avrà lo stesso tempo di accesso. Per questo random access memory. Abbiamo varie tecnologie che vanno a costituire le nostre RAM. Le principali sono le S RAM e le D RAM. D RAM sta per Dynamic RAM, diciamo, è la RAM un po' più economica, è costituita da una sorta di array di condensatori e transistor. Ogni capacità eh può eh mantenere l'informazione uno se carica, invece mantenere il nostro zero logico se scarica. Il problema vero è che eh durante il funzionamento di questo tipo di circuito abbiamo bisogno di una serie di cicli di refresh, delle capacità per tenerle cariche e quindi vanno a rallentare eh questo tipo questa tipologia di memoria. Invece le SR RAM che sta per static RAM sono costituite da flip flop. nello specifico di solito vengono utilizzati i flip flop di tipo di quello quelli che abbiamo visto nei video precedenti quando abbiamo parlato, insomma, dei registri. Un esempio è l'HM62256B che è una RAM, una static RAM con 15 linee di indirizzo, quindi abbiamo 32 KB di informazione. Ovviamente vale lo stesso discorso detto per la ROM AT28C64, cioè abbiamo 8 bit di informazioni che vanno a costituire 1 byte per questo 32 KB. Qui ho due chip di memoria. Questa qui è una S RAM, è la nostra HM62256B. Invece questa è la nostra Eeprom, la T28C64. Li riconosco essenzialmente dalla sigla, non so se riuscite a vederla, hanno le sigle diverse che mi fanno capire che tipologia di chip è. Ora, poi nel corso dei prossimi video proviamo a utilizzarle, vediamo effettivamente come fare delle letture, delle scritture nel caso della della nostra RAM e invece come programmar la nostra EEPROM e poi come leggerla. A questo punto, dopo che abbiamo introdotto le memorie, possiamo parlare di architetture. Esistono varie architetture di calcolatori elettronici, ma le più diffuse, le più utilizzate anche a livello commerciale sono quella di V Newman e quella di Harvard. Io partirei da un piccolo preambolo storico. Diciamo subito che l'architettura di Harvard è stata inventata dalla medesima università. Il primo computer ad essere costruito fu l'Harvard Mark 1 che fu completato nel 1943, era un computer utilizzato dalla Marina Militare. Invece l'architettura di Von Newman prende il nome da John Von Newman che teorizzò il funzionamento di questa tipologia di architettura e collaborò alla costruzione del computer Edwach che fu completato nel 1949. Questo computer veniva utilizzato per ambito di ricerca e militare. La principale differenza tra le due architetture è la gestione della memoria. Ho schematizzato la CPU con questo quadratino che contiene al suo interno i componenti principali, cioè l'alu, i registri e la control unit. Se vediamo le due architetture a confronto, a colpo d'occhio vediamo che nell'architettura di Harvard abbiamo due memorie separate, invece nell'architettura di di V Newman abbiamo un'unica memoria. Partiamo dalla Von Newman. Di fatto la CPU parla con un'unica memoria, detta anche memoria centrale, che contiene tutta la parte relativa al codice e i dati. Il codice sarebbero le istruzioni che vengono eseguite dalla CPU durante il suo flusso di lavoro e i dati a cui fanno riferimento queste istruzioni. Abbiamo quindi un unico bass dati collegato alla memoria e un unico bass di indirizzi che va a pilotare la memoria. Invece l'architettura di Harvard separa fisicamente e logicamente la memoria relativa al codice, quindi alle istruzioni che esegue la CPU, dalla memoria di dati. Quindi capiamo subito che abbiamo due bass di indirizzi perché dobbiamo gestire due memorie separatamente e poi abbiamo un bass per i dati e un bass per il codice. Quali sono i vantaggi e gli svantaggi delle due architetture? Sicuramente la Harvard è più veloce, può essere più veloce in alcune operazioni perché possiamo fare contemporaneamente la lettura sulla memoria del codice e sulla memoria dei dati. Però il problema è che aumenta la complessità di questa tipologia di processori perché a livello circuitale dobbiamo gestire due memorie, quindi due bass indirizz, due bass dati, una control unit che deve generare più segnali di controllo per gestire simultaneamente le due memorie e quindi paghiamo un po' eh la velocità con questo aumento di complessità. L'architettura di Von Newman invece resta più semplice perché abbiamo un unico indirizzo eh di memoria, dato che abbiamo una sola memoria, un bass dati, la control Unit sarà più leggera, sarà più dovrà generare meno segnali di controllo, però abbiamo un collo di bottiglia che viene appunto nominato il collo di bottiglia di Von New Human. Il problema è che, dato che abbiamo un'unica memoria, per fare una lettura del codice più i dati, dobbiamo eseguire più operazioni, bisogna impiegare più tempo perché non possiamo farle in maniera simultanea. C'è da dire che a livello commerciale i computer che utilizziamo oggi eh utilizzano la Von Newman. La Harvard invece viene utilizzata su microcontrollori principalmente o comunque tutta una serie di dispositivi che lavorano in real time come i processori audio, quindi dei dispositivi che prendono un segnale audio. Abbiamo lì bisogno di delay bassissimi tra l'elaborazione del segnale e quindi lì vince l'architettura di Harvard perché comunque è più veloce. Altra cosa da dire sulle memorie che utilizzano le due architetture è che nella Harvard di solito il codice è presente all'interno di una ROM ed ha senso perché comunque deve essere solo letto il codice, è una memoria di sola lettura. Storicamente nei computer primordiali comunque la memoria di codice era anche data da schede perforate. Inserivi la scheda perforata e veniva caricato il programma. Invece la memoria dati è una RAM solitamente. Invece nella human RAM che contiene codice dati. C'è da dire che eh dobbiamo fare comunque una fase di Bootstrap nella Von Newman perché dobbiamo prendere e caricare il programma più i dati all'interno della memoria RAM. Solitamente si usa una ROM. All'interno della ROM abbiamo il codice, viene presa e viene spostata all'interno della RAM. Una volta che si trova il programma più i dati all'interno della RAM, può effettivamente avvenire l'esecuzione e la CPU può iniziare a leggere dalla memoria le prime istruzioni. C'è da dire che questo funzionamento rimane anche nei nei computer moderni perché comunque il sistema operativo di solito è all'interno dell'hardisk e comunque deve essere caricato tutto in memoria prima di utilizzare il computer. Detto ciò, eh all'interno del nostro progetto costruiremo una CPU che utilizza l'architettura di Von Newman. Altra cosa da dire che vale per entrambe le architetture è che la CPU utilizza un ciclo macchina per eseguire un'istruzione formato da tre macromenti. Abbiamo una prima fase di fetch, una fase di decode e una fase di execute. Il fetch è quella fasce quella fase in cui la CPU va a prendere dalla memoria l'istruzione e tutto ciò che occorre per l'esecuzione della dell'istruzione. Quindi in questa fase di freccia andiamo a prendere l'istruzione da eseguire. Poi abbiamo una fase di decode dove la Control Unit solitamente va a preparare tutti i segnali di controllo che serviranno nella successiva fase di esecuzione e infine la fase di esecuzione che seguirà effettivamente l'operazione. Ora diciamo subito che la cosa principale, la cosa che differisce maggiormente tra V New e Harvard è la fase di fetch, perché comunque andare a reperire l'istruzione da una sola memoria o comunque da due memorie nel caso di Harvard eh va a determinare comunque tutta una serie di operazioni che sono completamente diverse tra le due architetture. Io direi che per questo video è tutto. Diciamo che oggi è stata più una lezione teorica che pratica. Comunque nel prossimo video vorrei continuare a parlare dell'architettura di Evon Newman scendendo un po' più nel dettaglio, quindi vedendo come funzionano effettivamente tutti i processi che dovranno essere gestiti durante l'esecuzione di un'istruzione. Vedremo il funzionamento del program counter, del memory address register, del instruction register, del memory data register e così via. Inoltre proviamo anche a costruire il program counter e vedere effettivamente come collegarlo alla alla nostra RAM. Vi ricordo di commentare questo video, lasciare un mi piace e iscrivervi al mio canale in modo da supportare il mio lavoro.